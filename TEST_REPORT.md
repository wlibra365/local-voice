# 検証結果 — 2026-09-23（v0.5.0）

## 結論

DSP・WAVの単体テスト、Java構文解析、Android APKの生成・署名・zipalign・内容検査は成功。
**v0.5.0では話者モデル取り込みと切替基盤を実装。同梱は1話者であり、子供・アニメ調・大人の追加モデルは未調達。Android実機での取り込み、切替、再試聴、遅延は未検証。**
APKはデバッグ署名であり、公開用リリース署名ではない。
APK SHA-256: `a5f73594df703203b892ab7d903ff7f08021664ec9cbf428971fd92fba113373`

## 実行環境とコマンド

Linux / OpenJDK 17.0.20 / Android SDK command-line tools 12.0 / build-tools 35.0.0。
JDKにはjavac実行ファイルがないが、jdk.compilerモジュールが利用できたため `java com.sun.tools.javac.Main` でテストとAPKのJavaコンパイルを行った。

```bash
bash tests/run.sh
ANDROID_SDK_ROOT=/path/to/android-sdk ./build-apk.sh
```

最終実行：終了コード0。

## 自動テスト

| 対象 | 結果 | 検証内容 |
|---|---|---|
| Java DSP | PASS 18ケース | 44.1/48kHz、±12/±5/0半音、無音、ブロック分割、発声単位リセット、設定変更・出力範囲 |
| WAV実ファイルテスト | PASS | ヘッダ・PCMエンディアン、確定、復旧、奇数バイト切捨、空録音、破損ファイル保持 |
| Java構文解析 | PASS 17ソース | 構文のみ。Android依存型の解決はしない |
| LLVC接続ブリッジ | PASS 12ケース | 44.1/48kHz→16kHz、16サンプル先頭シフト、32サンプル文脈、208サンプル出力、定常時の無音穴なし、音色・息感操作、無音時の無雑音、再帰状態・リサンプラーの発声単位リセット |
| 公式LLVC ONNX変換 | PASS | `G_500000.pth`（39,489,146 bytes、公開SHA-256一致）を状態付きONNXへ変換。PyTorch/ONNX Runtime一致、20フレーム連続推論、240入力→208出力を確認 |
| 比較・モデル検証 | PASS | 遅延・音量合わせ、モデルハッシュ・形式・状態・フレーム長・ライセンス・ONNX一致結果の検査 |
| ONNX変換ツール | PASS | 状態ツリーの平坦化・復元。実モデル変換はモデル取得後に実施 |
| モデルZIP作成 | PASS | 3ファイル梱包、既存出力の保護、モデルSHA-256不一致の拒否 |
| APK内容 | PASS | aapt2リンク、D8 dex生成、v2/v3署名、zipalign、aapt2/apkanalyzer/unzip検査 |

Java DSPはJDKの境界チェックとテスト入力で検証した。Android実行時のGC・負荷・音切れは未検証。
女性寄せ（低域抑制・上位倍音・平滑化LPC包絡ワープ）はストレス入力とブロック分割テストに含めたが、実音声の聴感評価は未実施。
調声モードの再変換は単一の変換器を再利用し、各発声前に内部状態を初期化する。リセット後に同一入力が同一出力になることをDSPとLLVC接続ブリッジで検証した。Android上のスライダー連続操作と画面離脱は実機確認が必要。
LLVCの先頭16サンプルと208サンプルホップを公式手順に合わせた。480サンプル端末ブロックとの単位差で周期的に無音が入り得たため、出力開始前に2ホップ相当の余裕を設けた。一定振幅モデルを模したテストで、起動後の定常出力に無音穴がないことを44.1/48kHzの両方で確認した。これは人声の聴感合格を意味しない。
「声質」「輪郭」「温かさ」はLLVC波形への周波数補正で反応し、「息感」は発声の包絡に追従する帯域制限ノイズを合成する。無音時の雑音ゼロと有声時の変化を自動検証した。LLVCモデルには可変ピッチの条件入力がなく、高さのUIはLLVC使用中に非表示にする。プリセットもモデルそのものの話者を変える操作ではない。
KoeAI公式LLVCチェックポイントから生成した同梱ONNXは、音声入力 `[1,1,240]`（32サンプル先読みを含む）に対して音声出力 `[1,1,208]` を返す。ONNX Runtime CPUで20フレームを状態継続して実行し、有限値と形状を確認した。追加モデルの端末側ZIP検査・試験推論・話者切替・推論速度・発熱・音質は実機未検証。
Lint CLIはGradleプロジェクトとして扱われるため、Wrapperなしの直接実行ではプロジェクト検査を完了できず、合格扱いにしていない。Javaコンパイルは `-Xlint:all -Werror` で通過している。

## ピッチ測定

220Hz正弦波、2秒入力、最初の0.5秒を除外。90〜470Hzを0.5Hz刻みで探索した主ピーク。
44.1kHzと48kHzで同じ測定結果。

| 設定 | 理論周波数 | 主ピーク |
|---|---:|---:|
| -12半音 | 110.000Hz | 107.5Hz |
| -5半音 | 164.814Hz | 163.5Hz |
| 0半音 | 220.000Hz | 220.0Hz |
| +5半音 | 293.665Hz | 295.5Hz |
| +12半音 | 440.000Hz | 445.0Hz |

判定条件はこの試験音に対して理論値との差8Hz未満。
短い40ms幅での初回試験は-12半音が120Hzとなり不合格だった。80ms幅に調整し上記結果を得た。
この誤差・遅延とのトレードオフは方式の制約であり、音楽用の精密ピッチや自然な声質変換を保証しない。
実際の声、他の基本周波数、子音、雑音、連続会話の聴感評価は未実施。

## 残る確認

- APKは `app/build/outputs/apk/debug/local-voice-debug.apk` に生成済み。デバッグ署名、v2/v3署名、zipalignを検証済み。
- Android APIの型を解決するAPKビルドは通過したが、v0.5.0の実機インストール・起動は未実施。
- ネイティブJNI・NDKは使用せず、Java DSPをAPKへ収録した。
- 変声コントロールと録音ファイルをMainActivity内の上下2パネルに統合した。
- AndroidのAPI互換・Lint・通知・AudioRecord/AudioTrack・機器切替・電池消費は未確認。
- 実機の往復遅延・音切れ・イヤホン抜去時の出力安全性は未測定。
- 2時間録音、容量不足、OS強制終了の実機試験は未実施。
- 全チェック項目は `DEVICE_CHECKLIST.md` に記載。

実機受入を通すまでは、実用版／実機動作確認済みAPKと表記しない。
# Ⅲ教師LLVCの事前検証追記

`tools/check_llvc_teacher_pairs.py --self-test` で、音声ペア正常系・変換音欠損・20ms超の長さずれを検証しました。`bash tests/run.sh` の全項目は通過。Ⅲの重み・音声素材・GPUが現環境にないため、教師音声生成、LLVC学習、Ⅲ声の音質比較、実機確認は未実施です。APKと同梱モデルは変更していません。

学習環境実装追記：`tools/segment_llvc_pairs.py` の音声分割・上書き防止、`tools/llvc_training.py` の公式コマンド引数・設定生成・再開保護を自己テストし、`bash tests/run.sh` 全項目が通過。GPU/コンテナ依存ライブラリの実ビルドとモデル学習は現環境では未実施。
