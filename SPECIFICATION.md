# ローカル変声 — 旧初期版仕様 v0.2

この文書は旧DSP試作の実装記録。現行の製品仕様は同梱する `female-voice-redesign-spec-v1.md` を正とし、男性声から透明な女性声へのモデル変換へ移行中です。

## 1. 目的と対象

Android端末のマイク音声をローカルで逐次加工し、変声後の音声を同時録音する。
ユーザー操作で録音を開始し、必要なときだけイヤホンで確認する。
対象はAndroid 14。ソース上の最低APIは26だが、他バージョンを動作確認済みとはしない。
AI声質変換よりも小規模なDSP方式を先に実装する。

## 2. 決定事項

| 項目 | 仕様 |
|---|---|
| アプリ名 | ローカル変声 |
| ネットワーク | INTERNET権限なし、登録・課金・モデル取得なし |
| 音声入力 | 端末既定のマイク経路、VOICE_RECOGNITION |
| 内部形式 | モノラルPCM16、48kHz、入力初期化不可の場合44.1kHz |
| 処理単位 | 最大480サンプル、48kHz時10ms |
| 変声方式 | 二重遅延線＋クロスフェード式ピッチ加工 |
| 保存形式 | RIFF WAV / PCM16 / mono、圧縮なし |
| 保存対象 | 変声後必須、元音声は任意の別WAV |
| モニター | 初期OFF、有線／USBのみ、アプリ出力倍率0.5 |
| 通話への出力 | 対象外。他アプリのマイクに置き換えない |
| 録音の上限 | 1回2時間、容量不足なら早期停止 |
| ファイル名 | 日時＋ランダム識別子＋voice/original |
| 自動再開 | なし。システム終了・音声フォーカス喪失後は手動再開 |
| 保存場所 | アプリ専用内部領域、ユーザー操作でSAF書出 |

## 3. 操作とパラメータ

| 項目 | 範囲／初期値 | 挙動 |
|---|---|---|
| ピッチ | -12〜+12半音、0 | 音高を加工。声の響きも変わる |
| 音色 | -100〜+100、0 | 暗い〜明るい。独立フォルマントではない |
| 女性寄せ | 0〜100%、100 | 低域抑制・上位倍音強調・短フレームLPCスペクトル包絡ワープを加える女性向け声質補正 |
| ロボット | 0〜100%、0 | 70Hzリング変調とのブレンド |
| ゲイン | -18〜+12dB、0 | 変声後の録音音量に作用 |
| ノイズゲート | ON | 無音付近を減衰。AI雑音除去ではない |
| モニター | OFF | 録音には影響させず、聴き返しのみ切替 |
| 元音声保存 | OFF | 開始前のみ切替、前回値を保持 |

高さ・ゲイン・音色・ロボット・ゲートは約10ms時定数で平滑化。
ピッチの切替時に音色が揺れる可能性があり、歌唱用の高精度変換を保証しない。
標準／高め(+5半音)／低め(-5半音)／機械の4プリセット。
スライダーとゲート・元音声保存設定は再起動後に保持。モニターONは新規起動時には保持しない。

## 4. 画面

画面は1画面の上下2パネル構成とする。上パネルは変声コントロール、下パネルは録音ファイルで、同時に表示する。

上パネルは、待機／準備／録音／確定／停止またはエラーの状態、録音時間、サンプルレート、音量メーター、出力アンダーラン数を表示。
アンダーラン数は録音の欠損数でも往復遅延でもない。
録音開始／停止、パラメータ、プリセット、元音声保存切替を配置。

下パネルは録音一覧、新しい順の表示、再生、書出、削除のみを配置する。
一覧は新しい順。最初は30件を表示、残りは追加表示。
録音ごとに再生・書出・削除。削除は確認ダイアログ必須。
録音中のファイル再生は不可。アプリを離れると再生を停止する。

## 5. データ処理

1. 録音専用スレッドがマイクを読み出す。
2. Java DSPでDC低減、簡易ゲート、ピッチ、女性寄せ（低域抑制・上位倍音・LPC包絡ワープ）、音色、ロボット、ゲイン、出力制限を適用。
3. 事前確保したパケットへコピーし、別スレッドでWAVに書く。
4. 同じ変声音声をノンブロッキングでAudioTrackへ渡す。
5. モニター側の混雑は音切れ表示とし、ディスク用録音データの保存は優先する。

128個の固定パケットを循環使用。48kHzで約1.28秒の保存待ちを吸収する。
保存キュー枯渇・ディスク書込失敗では録音を停止し、理由を表示。無制限のメモリ増加は禁止。
WAV書出は別スレッド。Java配列処理は完全なハードリアルタイム設計とはしない。

## 6. 遅延と品質の扱い

20〜50msの往復遅延を保証しない。選択したピッチ方式は80msの遅延線幅を使い、標準声では約40msの処理遅延がある。
変声時の読出位置は約0〜80msの範囲で変化する。これに端末の入力・出力バッファが加わる。
実際の往復遅延は実機測定し、録音データの長さと区別する。
短い遅延線では試験音のピッチ誤差が大きかったため、初期版は80ms幅に調整した。
ピッチ加工は周期的な揺れ・粒状感を持つ場合がある。LPCワープは20%上方への固定倍率を基準にした軽量処理で、話者ごとの完全なフォルマント変換や高品位な自然声は受入条件に含めない。
元音声と変声音声は同じ入力区間を保存するが、処理遅延補償をしないため波形の内容は時間的にずれる。
Bluetoothモニターは本版では対応しない。USB経路も機器ごとの動作確認が必要。

## 7. 録音・復旧

開始時に空き64MiB以上を要求。録音中は5秒ごとに調べ、16MiB未満で停止。
48kHz/mono/PCM16は約5.76MB/分、2時間で約691MB。元音声も保存すると約2倍。
書込中は `.wav.part`、正常終了時にヘッダと長さを書き戻し、同期後に `.wav` にリネーム。
次の起動／録音開始時に未確定ファイルを回収し、完了済みPCM部分を `_recovered.wav` にする。
ヘッダ未作成・未知の形式など安全に復旧できないファイルは消さずに保持する。
電源断・ストレージ故障では保存済み部分まで失う可能性があり、完全復旧は保証しない。

## 8. Android権限とライフサイクル

- RECORD_AUDIO：開始時に要求。拒否された場合は録音しない。
- POST_NOTIFICATIONS：Android 13以上で要求。拒否だけで録音を禁止しない。
- FOREGROUND_SERVICE / FOREGROUND_SERVICE_MICROPHONE：録音中通知とマイク種別を宣言。
- WAKE_LOCK：画面消灯中の処理用に時間制限付きで取得し、サービス終了時解放。
- 通信権限・ストレージ全件アクセス・電話権限は要求しない。

サービス開始はアプリ画面の録音ボタンからのみ。START_NOT_STICKYにより勝手な再録音を避ける。
音声フォーカス喪失時は停止・保存。イヤホン抜去のnoisyイベントでは停止。
ルーティング変更時に一旦消音し、実際の出力先が有線／USBのときだけモニターを許可。
端末依存の切替遅延・一瞬の漏れがないか、必ず実機で確認する。
電話着信・マイクのプライバシースイッチ・別アプリの録音との競合についても実機試験を必須とする。

## 9. オフラインとプライバシー

アプリ自身による音声の送信・バックアップ・ログ収集はしない。Android自動バックアップを無効にする。
WAVは暗号化せずアプリ専用領域に保存。端末ロックとOSの分離に依存する。
SAFで外部保存すると、その保存先の権限・同期設定が適用される。
端末移行ツールなどOS／メーカー側挙動まで一律に禁止する保証はしない。
他人の音声を録音する場合は必要な了承を得て利用する。

## 10. 本版で含めないもの

AIモデル・学習・声クローン、独立フォルマント、M4A/MP3、入力機器選択、モニター専用モード、録音一時停止、波形編集、リアルタイム文字起こし、広告、課金、通話アプリ連携。
これらは未実装であり、UIだけのダミー機能も置かない。

## 11. 受入条件

自動検証：DSPの音高変化、無音、ブロック分割一貫性、範囲内出力、サニタイザ、WAV形式と復旧、Java構文解析。
出荷前検証：AndroidビルドとLint、実機起動、権限拒否、10分以上の録音、画面消灯、回転、録音中パラメータ変更、抜去、着信、容量不足、強制終了復旧、再生・書出、機内モード動作。
**自動テストだけで「実機で使える完成版」とは判定しない。**

## 12. 参照した公式資料

- AudioRecord: https://developer.android.com/reference/android/media/AudioRecord
- AudioTrack: https://developer.android.com/reference/android/media/AudioTrack
- Foreground service types: https://developer.android.com/develop/background-work/services/fgs/service-types
- Android Gradle Plugin 8.7: https://developer.android.com/build/releases/agp-8-7-0-release-notes

参照確認日：2026-09-22。ライブラリ最新追随より、上記固定構成での再現性を優先する。
